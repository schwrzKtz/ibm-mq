MQのインフラ疎通確認（疎通テスト）を行う際に定義・整理しておくべき主な情報は以下の通りです。

### 1\. 接続先・ホスト情報

通信の基点と終点を特定するための基本情報です。

  * **ホスト名 / IPアドレス**: 送信側および受信側サーバの識別情報
  * **ポート番号**: リスナーが待ち受けるTCPポート（デフォルトは1414など）

### 2\. キューマネージャ（QM）情報

MQの中核となる管理オブジェクトの設定です。

  * **キューマネージャ名**: 接続先のQM名称
  * **デッドレターキュー（DLQ）**: 配信不能メッセージの格納先定義（使用の有無）
  * **CCSID**: 文字コード設定（例: UTF-8の場合は1208）

### 3\. チャネル情報

サーバ間の通信経路を定義する情報です。

  * **チャネル名**: 送信側と受信側で対になる名称
  * **チャネルタイプ**: 役割に応じた種別（SDR: 送信、RCVR: 受信、SVRCONN: サーバ接続など）
  * **トランスミッションキュー（伝送キュー）名**: 送信チャネルが紐づくキュー名

### 4\. キュー定義情報

メッセージの「入れ物」に関する設定です。

  * **ローカルキュー名**: 最終的なメッセージの格納先
  * **リモートキュー定義**: 対向先のQM名、ローカルキュー名、伝送キュー名の紐付け情報

### 5\. OS・ネットワーク関連設定

インフラレベルでの通信許可設定です。

  * **サービス名定義**: `/etc/services` へのポート登録
  * **アクセス権限**: 実行ユーザ（mqmなど）に対するMQオブジェクトの操作権限（setmqautコマンド等による設定）
  * **ファイアウォール/セキュリティグループ**: 対象ポートの通信許可（AWS環境等の場合）

### 6\. 監視・運用設定（必要に応じて）

疎通後の継続的な稼働確認に必要です。

  * **リスナーの起動設定**: 通信を受け付けるプロセスの自動起動設定
  * **監視シェル/ジョブ**: キューの滞留やチャネル状態を監視するためのスクリプト
